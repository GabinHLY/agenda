# Backend
JWT_SECRET=please-change-me
PORT=8787

# Frontend
# In development, API defaults to http://localhost:8787 if VITE_API_URL is not set.
# In production with Caddy + same domain, use /api (or leave empty in code if using reverse proxy).
VITE_API_URL=http://localhost:8787
